apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ${SUBSCRIPTION_PACKAGE}
  namespace: ${TAS_OPERATOR_NAMESPACE}
spec:
  channel: ${SUBSCRIPTION_CHANNEL}
  installPlanApproval: Automatic
  name: ${SUBSCRIPTION_PACKAGE}
  source: ${CATALOG_NAME}
  sourceNamespace: ${CATALOG_NAMESPACE}
